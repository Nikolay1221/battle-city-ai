# 🚀 Запуск Battle City AI в Google Colab

Вы можете скопировать этот код прямо в ячейку Colab и запустить.

```python
# 1. Клонируем репозиторий
!git clone https://github.com/Nikolay1221/battle-city-ai.git
%cd battle-city-ai

# 2. Устанавливаем системные библиотеки для эмулятора (NES)
# Без этого nes-py не соберется!
!apt-get update
!apt-get install -y libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev freetype2-demos

# 3. Устанавливаем Python-зависимости
!pip install -r requirements.txt

# 4. (Опционально) Если хотите продолжить обучение с вашей модели:
# Перетащите папку "models" (с файлом .zip) в папку "battle-city-ai" в файловом менеджере Colab слева.
# Если начнете без модели, скрипт может упасть с SAFETY STOP (так как ALLOW_NEW_MODEL=False).
# Чтобы разрешить старт с нуля, отредактируйте config.py прямо в Colab (двойной клик по файлу слева) и поставьте ALLOW_NEW_MODEL = True.

# 5. Запускаем обучение!
!python train.py
```

## 💡 Важные моменты для Colab:
1.  **Смените среду на GPU:** В меню сверху: *Runtime -> Change runtime type -> T4 GPU*. Это ускорит обучение в 50 раз.
2.  **Защита от перезаписи:** Если вы запускаете с нуля, скрипт вылетит с ошибкой (как мы и настроили).  Зайдите в `config.py` (слева в списке файлов) и поменяйте `ALLOW_NEW_MODEL = False` на `True`.
3.  **ROM игры:** Файл `BattleCity_fixed.nes` уже лежит в репозитории, загружать его отдельно не нужно.

## 📺 Как скачать результат?
После обучения скачайте папку `models/` (правой кнопкой -> Download) к себе на компьютер, чтобы запустить дома через `python test_model.py`.
